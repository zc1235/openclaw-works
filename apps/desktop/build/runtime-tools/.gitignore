# Downloaded at build time (see desktop-build-windows.yml). Keep the directory
# tracked (via .gitkeep) so electron-builder's extraResources source exists,
# but never commit the large node/python payloads.
*
!.gitignore
!.gitkeep
